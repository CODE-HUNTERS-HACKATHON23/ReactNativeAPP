import { StyleSheet } from "react-native";

const imageBackgroundStyle = StyleSheet.create({
    image:{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#18BBEA',
    }
})

export {imageBackgroundStyle}